{
  "name": "neurolab",
  "scripts": {
    "start": "echo \"no task code\" && exit 1",
    "start:ts": "tsc -w",
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}
